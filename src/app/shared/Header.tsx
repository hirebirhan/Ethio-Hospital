import Navbar from "../navBar";

const Header = () => {
    return (
        <header>
            <div className="header-content">
                <Navbar />
            </div>
        </header>
    );
};

export default Header;
